<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; display: flex; align-items: center; justify-content: center; height: 100vh; text-align: center; }
    </style>
</head>
<body>
    <%- include('../partials/home-logo') %>
    <div>
        <h1 class="display-3 fw-bold text-secondary">⏳</h1>
        <h2 class="fw-bold mt-3">Results Pending</h2>
        <p class="lead">The election results for <strong><%= room.title %></strong> have not been published yet.</p>
        <p class="text-muted">Please wait for the election organizer to publish the results.</p>
        <a href="/" class="btn btn-primary mt-3">Back to Home</a>
    </div>
</body>
</html>
